from setuptools import setup, find_packages

setup(
    name='pythonScrapeWithRequest',
    packages=find_packages(),
    version='0.0.0',
    install_requires=[
        'pandas',
        'orjson',
    ]
)
